# 단위 테스트용 의존성 — CI 친화적 (torch/sentence-transformers/transformers 같은
# GPU/ML 의존성 제외). backend.* 의 import 가 동작하는 데 필요한 최소 셋.
# 실제 학습/임베딩이 필요한 통합 테스트는 별도 marker 로 분리 (현재는 없음).

fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1

sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0

qdrant-client>=1.12.0

httpx>=0.27.0
orjson>=3.10.12
loguru>=0.7.2
tenacity>=9.0.0
python-dateutil>=2.9.0

# LLM provider SDK — 모듈 import 단계에서 끌어옴 (호출은 안 함)
openai>=1.55.0
anthropic>=0.40.0

# HTML 추출
trafilatura>=2.0.0
readability-lxml>=0.8.1
lxml>=5.3.0
lxml_html_clean
beautifulsoup4>=4.12.3
markdownify>=0.13.1

# PDF
pypdf>=5.1.0
pymupdf>=1.24.13

# YouTube
yt-dlp>=2024.10.0
youtube-transcript-api>=0.6.3

# Embedding smoke (CPU 가능, MiniLM-L6-v2 사용 — bge-m3 같은 무거운 모델은 production 만)
# torch 는 CPU 빌드만 — CI 에서는 ci.yml 의 --index-url 로 강제 (PyPI 기본은 GPU 시도).
sentence-transformers>=3.3.0
torch>=2.5.0

# Tests
pytest>=8.3.0
pytest-asyncio>=0.24.0
